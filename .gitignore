# Secrets / credentials
.env
.env.*

# Stale local WordPress snapshot — production is fully migrated off it and
# the live server's copy is deleted; purged from git history (filter-repo,
# 2026-08-29) since it carried plaintext DB credentials. Never re-add.
public_html/

# Site/DB backups — large binaries, contain full DB dumps
*.wpress
*.sql
db_backup_*.sql
www_backup_*.zip

# Generated analysis output — regeneratable, not source
graphify-out/

# Claude Code runtime state
.claude/*.lock
